	1. Данный документ описывает шаблон конфигурационных файлов для системы 
	мониторинга.
	
	2. Конфигурационный файл записывается в формате json. Доступ к этому файлу 
	должны иметь все программы в системе на чтение и не может быть модифицирован
	в процессе работы системы. Местоположение и название конфигурационного 
	файла должно быть фиксированым "/data/model/conf.json" . Некотроые поля в 
	конфигурационном файле являются обязательными, а некоторые нет. Также 
	допускаются javascript-скрипты в качестве значений. Такие поля должны иметь
	ключь script.
	
	3.Формат. Символом * помечены обязательные поля
	{
		"vendor":"...", // производитель *
		"product":"...", // название системы *
		"version":"...", // версия системы *
		"poll_interval":N, // интервал между опросами в милисекундах *
		
		"input":
		{
		    "values":
			[
				{
					"description":"......", // описание параметра
					"name":"....", // идентификатор. Ключь для записи в базу данных. *
					"addr":N // адрес в сети modbus устройства *
					"reg":N, // номер регистра в устройстве *
					"bit":N, // номер бита для битовых значений
					"convert":"function(){}", // функция преобразования значения регистра
					"intervals":[{"min":N,"max":N},....] // список допустимых значений
				},
				..........
			],
			"script":"function(){.....}" // скрипт вызываемый в случае обнаружения 
			                             // недопустимых значений
		}
		"output":
		{
			"values":
			[
				{
					"description":"......", // описание параметра
					"name":"....", // идентификатор. Ключь для записи в базу данных. *
					"addr":N // адрес в сети modbus устройства *
					"reg":N, // номер регистра в устройстве *
					"bit":N, // номер бита для битовых значений
					"intervals":[{"min":N,"max":N},....] // список допустимых значений
				},
				..........
			],
			"script":"function(){.....}" // скрипт вызываемый в случае обнаружения 
			                             // недопустимых значений
		},
		"coils":
		[
			{
				"description":"......", // описание параметра
				"name":"....", // идентификатор. Ключь для записи в базу данных. *
				"addr":N // адрес в сети modbus устройства *
				"reg":N, // номер регистра в устройстве *
				},
				..........
		]
	}
